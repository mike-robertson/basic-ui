declare type ButtonProps = {
  onClick: void,
  classes: any,
  className: string,
  tag: string | void,
  children?: React.Element<any>,
  // eslint-disable-next-line no-unused-vars
  sheet: Object,
  buttonProps?: any,
}
